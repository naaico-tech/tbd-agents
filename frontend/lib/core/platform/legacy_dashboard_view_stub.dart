import 'package:flutter/widgets.dart';

class LegacyDashboardView extends StatelessWidget {
  const LegacyDashboardView({super.key, required this.uri});

  final Uri uri;

  @override
  Widget build(BuildContext context) => const SizedBox.expand();
}
